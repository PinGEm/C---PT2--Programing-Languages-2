#ifndef HELPER_H  // IF this header "HELPER_H" has not been defined before
#define HELPER_H  // THEN DEFINE IT.
#include <string> // includes the string datatype.

	std::string showName(std::string name); // defining a function with a return data type of string. we will also make this function require a parameter, which means we have to pass a string value to it if we want to call it.
	int addition_BITWISE(int addened_one, int addend_two); // defining a function with a return data type of integer. we will also make this function require two (2) parameters, 
														   // which means we will to pass TWO integer values to it if we want to call it.

#endif // end the if header guard.